version: "3"
services:
  adventure-cli:
    build:
      context: ./cli
      dockerfile: ${DOCKER_FILE:-Dockerfile}
    stdin_open: true # docker run -i
    tty: true # docker run -t
    command: /bin/sh
    volumes:
      - ./cli:/usr/src/app
    env_file:
      - ./.env
